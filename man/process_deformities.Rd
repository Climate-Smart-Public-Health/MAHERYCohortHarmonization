% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_deformities.R
\name{process_deformities}
\alias{process_deformities}
\title{Process Visual Deformities Data}
\usage{
process_deformities(df)
}
\arguments{
\item{df}{A data frame containing the columns \code{visual_health_deformities} and \code{other_visual_health_deformities}.}
}
\value{
A modified data frame with an added \code{deformities_clean} character column, containing cleaned and translated deformity labels.
}
\description{
This function harmonizes and cleans deformity-related health data collected from
two columns: \code{visual_health_deformities} and \code{other_visual_health_deformities}.
It merges the two sources, removes placeholder text, translates free-text values
into standardized English terms, and returns a new column \code{deformities_clean}.
}
\details{
Specific cleaning steps include:
\itemize{
\item Merging both \code{visual_health_deformities} and \code{other_visual_health_deformities} when both are present
\item Lowercasing and removing prefixes like "other deformities, "
\item Translating common Malagasy or ambiguous phrases into standardized terms:
\itemize{
\item \code{"balana"} → \code{"strabismus"}
\item \code{"simba maso ilany"} → \code{"one_eye_non_functional"}
\item \code{"tongotra"} → \code{"malformed_foot"}
\item \code{"kondohalitry"} → \code{"severe_bowleggedness"}
\item \code{"missing teeth"} → \code{"missing_teeth"}
}
}
}
\examples{
df <- tibble(
  visual_health_deformities = c("Balana", "Simba maso ilany", NA),
  other_visual_health_deformities = c(NA, "Tongotra", "Kondohalitry, moa")
)
process_deformities(df)
}
